name: Cron subscription job

on:
  schedule:
    - cron: "0 1 * * *"
  workflow_dispatch:

concurrency:
  group: cron-subscription-${{ github.event_name }}-${{ github.event.schedule || github.run_id }}
  cancel-in-progress: false

jobs:
  subscription:
    name: Run subscription cron
    runs-on: ubuntu-latest
    env:
      CRON_BASE_URL: ${{ vars.CRON_BASE_URL }}
    steps:
      - name: Validate base URL
        shell: bash
        run: |
          set -euo pipefail
          if [ -z "${CRON_BASE_URL:-}" ]; then
            echo "CRON_BASE_URL is not set."
            exit 1
          fi

      - name: Request subscription cron
        shell: bash
        run: |
          set -euo pipefail
          # Edge hosts can reset the connection after the JSON response is already sent.
          response_file="$(mktemp)"
          trap 'rm -f "$response_file"' EXIT
          http_code="$(
            curl --silent --show-error \
            --http1.1 \
            --connect-timeout 10 \
            --max-time 120 \
            --retry 5 \
            --retry-all-errors \
            --retry-delay 2 \
            --output "$response_file" \
            --write-out '%{http_code}' \
            "${CRON_BASE_URL%/}/api/cron/subscription" || true
          )"

          if [ "$http_code" = "200" ] && grep -Fq "Subscription update scheduled." "$response_file"; then
            exit 0
          fi

          cat "$response_file"
          exit 1
