import { PlaceholderPage } from "@/components/placeholder-page";

export default function SeriesPage() {
  return (
    <PlaceholderPage
      title="剧集"
      description="这里将展示剧集内容，后续接入季、集和追更状态。"
    />
  );
}
